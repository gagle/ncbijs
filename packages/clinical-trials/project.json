{
  "name": "@ncbijs/clinical-trials",
  "sourceRoot": "packages/clinical-trials/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/clinical-trials/tsconfig.build.json && node scripts/add-import-extensions.mjs packages/clinical-trials/dist"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --config packages/clinical-trials/vitest.config.ts" }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": { "lintFilePatterns": ["{projectRoot}/**/*.ts"] }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p packages/clinical-trials/tsconfig.json" }
    }
  }
}
