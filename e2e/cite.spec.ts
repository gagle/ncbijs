import { describe, it } from 'vitest';

describe('Citation Exporter E2E', () => {
  describe('cite single article', () => {
    it('should generate APA citation for a known PMID', () => {});
    it('should generate CSL JSON for a known PMID', () => {});
    it('should generate BibTeX for a known PMID', () => {});
    it('should generate RIS for a known PMID', () => {});
  });

  describe('citeMany', () => {
    it('should generate citations for multiple PMIDs', () => {});
  });
});
