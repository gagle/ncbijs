import { describe, it } from 'vitest';

describe('PubTator E2E', () => {
  describe('entity search', () => {
    it('should find gene entities by name', () => {});
    it('should return entity matches with scores', () => {});
  });

  describe('export annotations', () => {
    it('should export BioC annotations for a known PMID', () => {});
  });

  describe('BioC REST', () => {
    it('should fetch PubMed article annotations via BioC', () => {});
  });
});
